import {
  getLoginUri,
  getAccessToken,
  logout,
  refreshAccessToken,
} from '@/api/oauth';

const HTTP = {
  getLoginUri,
  getAccessToken,
  logout,
  refreshAccessToken,
};

export default HTTP;
