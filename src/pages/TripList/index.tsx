import styled from 'styled-components';

const TripList = () => {
  return <LoginButtonBox>TripList Page 입니다.</LoginButtonBox>;
};

export default TripList;

const LoginButtonBox = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
`;
